def jump(nums):
    all_jumps = []
    step = 0
    target = len(nums)-1
    min_jump = -1
    recorder = 0
    for i in range(len(nums)):
        step += 1
        if (nums[i] > 1 and j < target){
            if (i + nums[j] >= target){
                step += 1
                all_jumps.append(step)
                recorder = 0
                continue
            }
        }
